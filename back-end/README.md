# Back end created using `express-generator`

# run following commands to start the back-end

- `yarn`
    -

# then run the following after packages have been installed in node_modules
-   `yarn start`
    -

